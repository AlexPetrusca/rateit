{{- if .Values.nginx.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: rateit-nginx-metrics
  labels:
    {{- include "rateit.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.nginx.service.type }}
  selector:
    {{- include "rateit.selectorLabels" . | nindent 4 }}
    component: nginx
  ports:
    - port: 9113
      targetPort: 9113
{{- end }}