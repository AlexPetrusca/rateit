spring:
  config:
    activate:
      on-profile: prod
  datasource:
    jdbc: postgresql://critic-postgresql:5432/postgres
  cloud:
    aws:
      s3:
        endpoint: http://${MINIO_HOST:critic-minio}:${MINIO_PORT:9000}

security:
  auth:
    cookie:
      secure: true

logging:
  level:
    root: INFO
